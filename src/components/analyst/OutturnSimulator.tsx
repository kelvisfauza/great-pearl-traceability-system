
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const OutturnSimulator = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Outturn Simulator</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-600">Outturn simulation functionality will be implemented here.</p>
      </CardContent>
    </Card>
  );
};

export default OutturnSimulator;
