
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";

const MarketMonitor = () => {
  return (
    <Card>
      <CardHeader>
        <CardTitle>Market Monitor</CardTitle>
      </CardHeader>
      <CardContent>
        <p className="text-gray-600">Market monitoring functionality will be implemented here.</p>
      </CardContent>
    </Card>
  );
};

export default MarketMonitor;
